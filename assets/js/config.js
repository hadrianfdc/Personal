
const CONFIG_CONSTANTS = {
  GEMINI_API_KEY: 'GEMINI_API_KEY_PLACEHOLDER',
  // Get this at https://web3forms.com — enter the email that should
  // receive contact form submissions, no account/billing required.
  // This key is meant to be public (it just routes submissions to your
  // inbox), unlike the other keys in this file.
  WEB3FORMS_ACCESS_KEY: 'WEB3FORMS_ACCESS_KEY_PLACEHOLDER',
};

window.CONFIG_CONSTANTS = CONFIG_CONSTANTS;